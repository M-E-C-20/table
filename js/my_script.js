$(document).ready(function () { // stops the script from running before the document has loaded
    $('h2').removeClass('underline'); // can be removed
    $('ul').removeClass('border'); // can be removed
})